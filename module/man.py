class Manual:
    mean_app = f"""Use spaces or comma to separate numbers (e.g. 1.1 1.2)"""
    
    spine_ht_loss_app = f"""Input height in centimeter and use two values to calculate mean height, separated by spaces or comma (e.g. 10 12)"""
    spine_ht_loss_app_ref = f"Mild (20-25%), Moderate (25-40%), Severe (>40%)"
    
    vol_app = f"""Input perpendicular diameters (cm) in 3 planes, separated by spaces or comma (e.g. 4.4 4.5 4.6)"""
    vol_app_ref = f"Normal (<25 ml), Prominent (25-40 ml), Enlarged (>40 ml)"